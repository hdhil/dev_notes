//TEMPLATE STRINGS

//instead of doing:
	const name = 'Harman';
	const age = 10;
	
	const greeting = "Hello " + name + "you seem to be " + age;

//we can do following:

const greetingBest = `Hello ${name} you seem to be ${age}`
